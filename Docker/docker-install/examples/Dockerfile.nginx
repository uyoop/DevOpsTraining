# Exemple 1 : Serveur web statique simple
FROM nginx:alpine

# Copier le contenu HTML
COPY ./html /usr/share/nginx/html

# Exposer le port 80
EXPOSE 80

# La commande par défaut de nginx:alpine est déjà correcte
