# CJ-DEVOPS

## Branches principales
- `docker` : TPs Docker 01 → 18 (stacks BookStack, NetBox, Prometheus, Harbor, Portainer).
- `ansible-automation` : Playbooks, rôles et TP Ansible.
- `vagrant-vms` : Environnements Vagrant multi-VM.
- `uyoop-app` : Application UyoopApp (classique + version dockerisée).
- `test` : Labs et brouillons techniques.
- `main` : Hub documentaire et pointeurs vers chaque branche.

## Navigation rapide
- Dossier `branches/` : snapshots consultables sans changer de branche Git.
- Branche active recommandée pour la formation Docker : `docker`.
