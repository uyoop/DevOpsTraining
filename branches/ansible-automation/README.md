# Branche ansible-automation

## Playbooks, rôles et TP Ansible (inventory, Vagrant, machines-test, roles).
- Contenus clés : `TP-Ansible/`, `Playbooks/`, `roles/`, `machines-test/`, `inventory.ini`.
- Référence pour la branche Git `ansible-automation` (snapshot consultable sans checkout).
